---
audio: false
generated: false
image: false
lang: ja
layout: post
title: AI向けソフトウェアエンジニアリングの最適化
translated: true
---

このブログでは、翻訳、プレイグラウンド、フロントマターのメンテナンス、Telegramボットなどの作業を支援するために、数百のスクリプトを活用しました。このような開発アプローチは、AI最適化されたソフトウェアエンジニアリングの未来を示しているかもしれません。

私はPythonモジュールの機能にあまり依存せず、大規模なJava Springプロジェクトのようにコードを構造化することも望みません。

以前のチームリーダーに、彼らの銀行アーキテクチャは素晴らしいが、もし今日から始めるなら、LLM/AI最適化を検討すると伝えました。これは、より良いコンテキストエンジニアリング、改善されたコンテキストの分離、AIファースト思考を人間中心の設計よりも優先することを意味します。Springは複数のレベルと良い抽象化を提供しますが、LLM/AIにとってはナビゲーションが難しい場合があります。

私は、よりフラットな構造を目指すべきだと考えています。これは、2つのレベルを使用することです。第一レベルが第二レベルを呼び出します。関数では、50のネストされたレベルやスタックを持つよりも、50の関数を直接呼び出す方が良いです。AI/LLMは過度に複雑でネストされた構造を判断または推論するのに苦労しますが、100～200行のコードの小さな関数を処理するのに優れています。Pythonは他のファイルから呼び出しやインポートを行うのに適しています。

PythonコードがJavaよりも簡単な理由の一つは、依存関係管理が簡単であることです。依存関係を追加するには、`pip install`を使用するだけです。Mavenでは、依存関係をPOM XMLファイルに記述し、`mvn compile`を使用してMavenが依存関係をダウンロードさせる必要があります。

Pythonのシンプルさのもう一つの理由は、コードを直接実行できることです。

Java 11からは、`java`コマンドを使用して単一ファイルのソースコードプログラムを直接実行できますが、`javac`を使用して別途コンパイルする必要はありません。ただし、Javaプロジェクトは大きいことが多いため、`mvn spring-boot:run`といくつかのプロパティ設定を使用して実行する必要があります。

Pythonのモジュール設計が簡単な第三の理由は、`from`と`import`を使用して他のファイルからコードを簡単にインポートできることです。

現在、多くのAIチャットボットは、Grokのようなチャットボットウィンドウ内でPythonコードを直接実行できます。

100のJavaファイル（各ファイル1000行程度）と、いくつかのシンプルなPythonスクリプトを比較するのは公平ではありません。このようなプロジェクトでは、1000のPythonファイル（各ファイル100行程度）を持つ方が好ましいです。

コードの行や関数を編集することは許容されます。しかし、どこを選択するかを知る必要があります。なぜAIにこの作業を任せないのでしょうか？私たちの生活をより簡単にするために、すべてのコードを選択し、AI/LLMに編集方法を伝えるだけで済みます。

Pythonでは、`if __name__ == "__main__":`を使用してファイル内の関数を実行およびテストするのが簡単です。また、他のPythonファイルがこのファイル内の関数を呼び出すのも簡単で、テストを実行する必要はありません。

これは、AI最適化されたコンテキストエンジニアリングです。他の方法でアプローチできますか？AI/LLMは自己回帰的です。しかし、CopilotやClaude Codeを使用すると、AIソフトウェアエージェントがどのように私たちを支援するかはわかりません。彼らは私たちの代わりに考えるべきです。

ユーザー側で、コードを特別に配置してトークン使用量を減らすことはできますか？この点について、1000のPythonファイル（各ファイル100行）を持つアプローチは、この目的に適しています。なぜなら、関数やコードファイルを簡単に確認でき、他のPythonコードがそれらを呼び出す前に確認できるからです。

ただし、問題は、複数のコードファイルを同時に変更したい場合、簡単にはできないことです。簡単な方法として、コードをAIチャットボットにコピーし、これらのファイルのコードを編集する方法を教えてもらうことができます。

おそらく、関数やロジックを分離するために行数を使用する必要はありません。しかし、ロジックを小さな関数に分離する必要があります。これは、自然にロジックの種類で分離することで、より短く見えるようにすることができます。

なぜAI最適化されたソフトウェアエンジニアリングが必要なのですか？AIは強力なので、すべてをAI最適化し、AIがソフトウェアエンジニアリングをできるだけ支援できるようにするべきです。

これはコードだけでなく、テキストにも可能です。私たちが非常に厳格な編集者である場合、AIに大規模なテキストを一度に編集させたくないかもしれません。段落ごとに確認したいと考えるかもしれません。コードの場合、軽微なエラーやバグは許容できます。テキストの場合も、ほとんどの読者は厳格ではないため、許容できます。

しかし、コードは、時には軽微なエラーが大きなプロジェクトの完全な機能不全につながることがあるため、異なります。

XMLやYAMLファイルは、すでに高度に構造化されているため、それほど分離する必要はないかもしれません。

HTMLファイルについては、分離する必要があります。数百のJavaScriptファイルと数百のHTMLファイルを一緒に書くのではなく、1000行を超えるコードを作成するのを避けるために、JavaScriptの`import`を使用するべきです。JavaScriptコードについては、上記の方法で分離できます。

コードを構造化する方法は、AIがコードを簡単に追加、編集、削除、実行できるようにする必要があります。これが始まりです。AIがすべてのコードを簡単に生成または修正できる日を想像してください。世界は高度にデジタル化されます。

自分が100の大規模なソフトウェアプロジェクトを書き、他の人とAPIで接続できることを想像してください。これは私の日常のアジェンダです。私は今や、1000人の従業員を持つテック企業のような存在です。それらは私のニーズに合わせてカスタマイズされ、収益を生み出したり、私の利益のために費用を支出したりしています。これは本当に驚くべきことです。