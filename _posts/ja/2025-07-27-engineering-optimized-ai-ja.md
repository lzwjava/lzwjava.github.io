---
audio: false
generated: false
image: false
lang: ja
layout: post
title: AIのためのソフトウェアエンジニアリングの最適化
translated: true
---

このブログでは、翻訳、プレイグラウンド、フロントマターのメンテナンス、Telegramボットなどの作業を支援するために、数百のスクリプトを活用しました。この開発アプローチは、AIに最適化されたソフトウェアエンジニアリングの未来を表していると考えます。

私はPythonモジュールの機能にあまり依存せず、大規模なJava Springプロジェクトのようなコード構造を作りたいとは思いません。

私はキャリアを通じて多くのソフトウェアプロジェクトに携わりました。印象的な銀行アーキテクチャ、マイクロサービス、複数国間の重複を最小限に抑えた効果的な設計、Springを基盤とした堅牢な基盤フレームワーク、中央集権的な設定を持つ強力なガバナンスなどを見てきました。

これらの銀行アーキテクチャは印象的ですが、もし今日から始めるなら、LLMやAIに最適化することを検討します。これは、より良いコンテキストエンジニアリング、改善された分離の原則、AIファーストの思考を人間中心の設計よりも優先することを含みます。Springは複数のレイヤーと良い抽象化を提供しますが、LLMやAIがナビゲートするのは難しい場合があります。

私はフラットな構造を目指すべきだと考えます。これは、2つのレベルのみを使用することです。最初のレベルが2番目のレベルを呼び出します。関数では、50のネストされたレベルやスタックを持つよりも、50の関数を直接呼び出す方が良いです。AI/LLMは過度に複雑でネストされた構造を判断または推論するのに苦労しますが、100から200行のコードの小さな関数を処理するのに優れています。Pythonは他のファイルから呼び出しやインポートを行うのに適しています。

PythonコードがJavaよりも簡単な理由の1つは、依存関係管理が簡単であることです。依存関係を追加するには、`pip install`を使用するだけです。Mavenでは、依存関係をPOM XMLファイルに記述し、`mvn compile`を実行してMavenが依存関係をダウンロードさせる必要があります。

Pythonのシンプルさのもう1つの理由は、コードを直接実行できることです。

Java 11からは、`java`コマンドで単一ファイルのソースコードプログラムを直接実行できますが、Javaプロジェクトは大きいことが多いため、`mvn spring-boot:run`といくつかのプロパティ設定を使用して実行する必要があります。

Pythonのモジュール設計が簡単な3つ目の理由は、`from`と`import`を使用して他のファイルからコードを簡単にインポートできることです。

現在、多くのAIチャットボットは、Grokのようなチャットボットウィンドウ内でPythonコードを直接実行できます。

1000行のコードを持つ100のJavaファイルと、100行のコードを持つ1000のPythonスクリプトを比較するのは公平ではありません。この種のプロジェクトでは、1000行のコードを持つ1000のPythonファイルを持つ方を好みます。

コードの行や関数を編集することは許容されます。しかし、どこを選択するかを知る必要があります。なぜこの作業をAIに任せないのでしょうか？私たちの生活を楽にするために、コード全体を選択し、AI/LLMに編集方法を指示するだけで済みます。

Pythonでは、`if __name__ == "__main__":`を使用してファイル内の関数を実行してテストするのが簡単です。また、他のPythonファイルがこのファイル内の関数を呼び出すのも簡単で、テストを実行する必要はありません。

これは、AIに最適化されたコンテキストエンジニアリングです。他の方法でアプローチできるでしょうか？ AI/LLMは自己回帰的です。しかし、CopilotやClaude Codeを使用すると、AIソフトウェアエージェントがどのように私たちを支援するかはわかりません。彼らは私たちの代わりに考えるべきです。

ユーザーサイドで、トークン使用量を減らすためにコードを特別に配置できますか？ この点では、100行のコードを持つ1000のPythonファイルを持つアプローチがこの目的に適しています。なぜなら、他のPythonコードがそれらを呼び出す前に、関数とコードファイルを簡単に確認できるからです。

ただし、問題は、複数のコードファイルを同時に変更したい場合、それを簡単に行うことができないことです。簡単な方法として、コードをAIチャットボットにコピーし、それらのファイル内のコードを編集する方法を教えてもらうことができます。

おそらく、関数やロジックを分離するために行数を使用する必要はありません。しかし、ロジックを小さな関数に分離する必要があります。これは、自然にロジックの種類別に分離することで、より短く見えるようにすることができます。

なぜAIに最適化されたソフトウェアエンジニアリングが必要なのでしょうか？ AIは強力なので、すべてをAIに最適化し、AIがソフトウェアエンジニアリングをできるだけ支援できるようにするべきです。

これはコードだけでなく、テキストにも可能です。私たちが非常に厳格な編集者である場合、AIに大規模なテキストを一度に編集させたくないかもしれません。段落ごとに確認したいと考えるかもしれません。コードの場合、軽微なエラーやバグは許容できます。テキストの場合、ほとんどの読者は厳格ではないため、許容できます。

しかし、コードは異なり、時には軽微なエラーが大規模なプロジェクトの完全な機能不全につながることがあります。

XMLやYAMLファイルは、すでに高度に構造化されているため、それほど分離する必要はないかもしれません。

HTMLファイルについては、分離を行う必要があります。数百のJavaScriptファイルを数百のHTMLファイルと一緒に記述し、1000行を超えるコードを作成するのではなく、`import`を使用してJavaScriptを管理するべきです。JavaScriptコードについては、上記の方法で分離できます。

コードを構造化し、AIがコードを簡単に追加、編集、削除、実行できるようにする必要があります。これが始まりです。AIがすべてのコードを簡単に生成または修正できる日を想像してみてください。世界は高度にデジタル化されるでしょう。

自分が100の大規模なソフトウェアプロジェクトを書き、他者と接続するためのAPIを提供する様子を想像してみてください。これは私の日常のアジェンダを含み、私は今や1000人の従業員を持つテック企業のような存在です。それらは私のニーズに合わせてカスタマイズされ、収益を生み出すか、私の利益のために費やされます。これは本当に素晴らしいことです。