---
audio: false
generated: true
lang: hant
layout: post
title: AI優先架構現代化開發
translated: true
---

這篇文章是由Grok協助撰寫的，但其中的觀點是原創的。

---

### 評估您的觀點

我將逐一分析您的觀點，並根據當前的軟體工程趨勢、AI/LLM整合（例如GitHub Copilot、Cursor或自訂LLM代理工具）以及實際應用情況進行評估。您觸及了一個關鍵洞察：隨著AI成為開發流程的重要組成部分，架構必須在優先考慮AI友好性的同時，兼顧以人為本的設計。然而，並非所有觀點都能普遍適用——情境至關重要，簡單與可擴展性之間的平衡至關重要。我將概述同意、不同意和關鍵細節。

#### 1. 傳統銀行架構有其優點，但新專案應優先考慮AI/LLM優化：專注於上下文工程、清晰分離和AI優先設計
您的理論觀點正確。傳統架構，例如銀行業（假設指的是單體式或分層企業系統）的架構，在可靠性、安全性和人類維護方面表現優異。然而，隨著AI/LLM代理驅動代碼生成、除錯和重構，"AI優先"的思維越來越相關。這涉及為LLM的限制（例如GPT-4o的128k代幣限制）進行設計，通過模組化代碼以確保關鍵細節能夠在這些邊界內。

- **優點**：清晰的分離關注點（例如，明確的數據流、提示或API邊界）能讓AI更有效地推理。例如，AI工具如LangChain或自訂代理在明確定義的、隔離的上下文中表現更佳，而非混亂的邏輯。
- **細節**：以人為本的設計仍然至關重要——AI仍然需要人類監督複雜領域，例如金融，其中監管合規性和安全性至關重要。混合模型可能是最佳選擇：AI優化重複任務，人類優化關鍵邏輯。
- **總結**：大致同意；這種趨勢在AI驅動的微服務和無伺服器架構中已經顯現。

#### 2. Spring提供強大的抽象，但對AI/LLM理解構成挑戰
您在这一点上是正确的。Spring（以及类似的Java框架如Micronaut）在企业环境中表现出色，具有依赖注入、AOP和分层抽象（例如控制器->服务->存储库）等功能。虽然对于人类管理的大型团队来说非常理想，但这些特性可能会让LLMs感到不堪重负，因为它们充满了间接性和样板代码。

- **優點**：LLMs常常在深層呼叫堆疊或隱含行為（例如@Autowired註解）上表現不佳，導致幻覺或不完整分析。關於AI代碼生成的研究表明，過度抽象的代碼庫中錯誤率較高。
- **細節**：並非所有抽象都是有害的——例如，介面增強了可測試性，間接幫助AI在模擬生成等任務中。然而，過度分層會增加上下文，使邏輯追蹤對LLMs更加複雜。
- **總結**：強烈同意；正在轉向較輕量級的框架（例如Quarkus）或最小框架的方法，以提高AI相容性。

#### 3. 傾向於較扁平的結構，類似於扁平組織：限制在2層，第一層呼叫第二層，避免50層的深層堆疊
這是一個簡化的有吸引力的想法，雖然並非在所有情況下都是理想的。較扁平的結構（例如，頂層協調器呼叫多個小函數）減少了嵌套，幫助LLMs避免在複雜呼叫堆疊上進行推理錯誤。這與Python腳本中常見的簡單函數鏈接相似。

- **優點**：較扁平的代碼降低了AI的認知負擔——LLMs在線性或平行推理上表現更佳，而非深層遞迴。"扁平組織"的類比成立：像初創公司一樣，較扁平的代碼對AI修改更具適應性。
- **細節**：從單一點呼叫大量函數可能會導致"意大利麵碼"，除非有嚴格的組織（例如，明確的命名或模組化）。在較大的系統中，最小的層次結構（3-4層）可以防止混亂。雖然AI代理如Devin能夠處理扁平結構，但如果沒有適當的協調，可能會出現效能問題。
- **總結**：部分同意；在可行的地方扁平化是有益的，但必須測試可擴展性。這與AI驅動開發中的函數式編程趨勢相符。

#### 4. AI/LLMs在處理複雜嵌套結構時遇到困難，但在小型函數（100-200行）中表現出色；Python的呼叫和匯入系統支持這一點
您在这一点上是正确的。当前的模型（例如Claude 3.5、GPT-4）擅长聚焦、封闭的任务，但在处理复杂性时表现不佳——上下文超过约500行时，由于代币限制和注意力分散，错误率会上升。

- **優點**：小型函數（100-200行）對AI來說是最佳的：容易提示、生成或重構。Python的匯入系統（例如`from module import func`）促進了模組化，使其比Java的類中心結構更適合AI。
- **細節**：雖然LLMs正在進步（例如，通過鏈式思考提示），但嵌套邏輯仍然是一個挑戰。Python的靈活性有所幫助，但靜態類型（例如TypeScript）也可以通過提供明確的提示來幫助AI。
- **總結**：強烈同意；這解釋了為什麼ML/AI生態系統（例如Hugging Face庫）通常採用Python的模組化風格。

#### 5. 將Java大型文件拆分為更多函數以便於測試/驗證；Java專案應模仿Python的結構
這是一個實用的方向。大型、單體式的Java類（例如1000+行）對人類和AI來說都是具有挑戰性的，而將其拆分為較小的文件/函數可以提高顆粒度。

- **優點**：較小的單元簡化了單元測試（例如使用JUnit）和驗證（AI可以專注於一個函數），這與Python的每個功能一個模組的方法相似。構建工具如Maven/Gradle可以無縫地適應這種方法。
- **細節**：Java的套件系統已經支持這一點，但從OOP單體的文化轉變是必要的。並非所有Java專案都應該模仿Python——性能關鍵型應用程序可能會從某些合併中受益。
- **總結**：同意；現代Java（例如Java 21中的記錄和密封類）正朝著這個方向發展。

#### 6. 在AI/LLM時代，程序化編程可能比OOP更出色
這是一個大膽但具體情境有效的觀點。程序化（或函數式）方法，以其強調簡單流程和純函數的特點，與LLM的優勢相符——生成線性代碼比處理OOP的狀態、繼承和多態更簡單。

- **優點**：OOP的抽象，例如深層繼承，常常使LLMs感到困惑，導致生成代碼中的錯誤。程序化代碼更可預測，並適合AI的模式匹配特性。像Rust（具有程序化特性）和Go（強調簡單性）這樣的語言反映了這種趨勢。
- **細節**：OOP並非過時——它在建模複雜領域（例如金融實體）方面仍然有效。混合方法（程序化核心與OOP包裝）可能是理想的。通過定制提示，LLMs可以管理OOP，但程序化減少了摩擦。
- **總結**：部分同意；程序化/函數式風格在AI工作流程中正在興起，但OOP在大型系統的長期維護方面仍然具有價值。

#### 7. 如VSCode或IntelliJ IDEA等IDE應提供AI輔助函數/方法編輯的快捷方式
您是對的，這將簡化工作流程。雖然當前的IDE提供部分支持（例如VSCode的"轉到定義"或Copilot的內聯建議），但專用的AI編輯快捷方式正在變得更加普遍。

- **優點**：選擇一個函數/方法並提示AI（例如"重構此函數"）將提高效率。工具如Cursor（基於VSCode構建）已經允許區域特定的AI編輯。
- **細節**：IntelliJ IDEA提供"提取方法"和AI助手（通過JetBrains AI），而VSCode擴展如GitHub Copilot Chat支持基於選擇的查詢。然而，原生的"AI編輯此函數"快捷方式將是一個有價值的添加——可能在未來的更新中出現。
- **總結**：同意；這與AI優先工具的興起趨勢相符。

### 總結
您的見解大多正確：採用AI優化的架構（扁平、模組化、小單元）解決了實際的LLM限制，並可以加速開發。然而，極端的設計，例如完全程序化或過度扁平化，可能會影響長期維護——平衡至關重要。在金融等領域，安全性和合規性可能需要某些抽象。您的觀點與AI輔助編程的新興最佳實踐相符，其中簡單化對人類和機器都有益。

如果您有具體的代碼範例或想探索AI優先的重新設計，請隨時提出！