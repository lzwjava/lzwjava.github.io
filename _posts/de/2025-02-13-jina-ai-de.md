---
audio: false
generated: false
image: false
lang: de
layout: post
title: '"Erkunden von AI-Such-APIs: Jina AI und Tavily-Integration"'
translated: true
---

### Jina AI

Dieses Python-Skript interagiert mit Jina-AI-Diensten unter Verwendung von API-Schlüsseln und Befehlszeilenargumenten. Es unterstützt zwei Hauptjobs: das Abrufen von Inhalten von einer URL und die Durchführung einer Suchabfrage. Das Skript holt sich den Jina-API-Schlüssel aus den Umgebungvariablen, um einen sicheren Zugriff auf die Dienste zu gewährleisten. Es verwendet die `requests`-Bibliothek, um HTTP-Anfragen zu stellen, und `base64`, um die Suchabfrage zu decodieren. Schließlich gibt das Skript die Antwort von Jina AI aus.

```python
import os
import requests
from dotenv import load_dotenv
import argparse
import base64

load_dotenv()

api_key = os.environ.get("JINA_API_KEY")
if not api_key:
    raise ValueError("JINA_API_KEY-Umgebungvariable nicht gesetzt.")

parser = argparse.ArgumentParser()
parser.add_argument("--job", type=str, choices=['url', 'search'], help="Job, der ausgeführten wird (url oder search)", erforderlich=True)
parser.add_argument("--input", type=str, help="Eingabe für den Job", erforderlich=True)
args = parser.parse_args()

if args.job == 'url':
    url = f'https://r.jina.ai/{args.input}'
    headers = {'Authorization': f'Bearer {api_key}'}
    print(f"URL: {url}")
    print(f"Headers: {headers}")
    response = requests.get(url, headers=headers)
    print(response.text)

elif args.job == 'search':
    frage = base64.b64decode(args.input).decode('utf-8', errors='ignore')
    url = f'https://s.jina.ai/{frage}'
    headers = {
        'Authorization': f'Bearer {api_key}',
        'X-Engine': 'direct',
        'X-Retain-Images': 'none'
    }
    print(f"URL: {url}")
    print(f"Headers: {headers}")
    response = requests.get(url, headers=headers)
    print(response.text)

else:
    print("Bitte spezifizieren Sie --job url oder --job search")

```

---

## Tavily AI

[Tavily](https://tavily.com) ist eine AI-Such-API, die speziell für LLM-Anwendungen entwickelt wurde. Sie bietet hochrelevante Suchergebnisse durch die Kombination von Websuche und AI-Verarbeitung.

Um Tavily zu verwenden, müssen Sie:

1. Auf der Website [tavily.com](https://tavily.com) einen API-Schlüssel anfordern.
2. Das Python-Paket installieren.