---
audio: false
generated: true
lang: zh
layout: post
title: 人工智能优先架构现代开发
translated: true
---

这篇文章是由Grok协助撰写的，但其中的观点是原创的。

---

### 对您观点的评估

我将逐一分析您的观点，并结合当前的软件工程趋势、AI/LLM集成（如GitHub Copilot、Cursor等工具或自定义LLM代理）以及实际应用进行评估。您触及了一个关键洞察：随着AI成为开发流程的重要组成部分，架构必须在AI友好性和以人为本的设计之间取得平衡。然而，并非所有想法都具有普遍适用性——上下文很重要，简单性和可扩展性之间的平衡至关重要。我将概述同意、不同意的观点以及关键细节。

#### 1. 传统银行架构有其优势，但新项目应优先考虑AI/LLM优化：重点关注上下文工程、清晰分离和AI优先设计
您的理论观点是正确的。传统架构，如银行系统（假设指单体或分层企业系统），在可靠性、安全性和人类可维护性方面表现出色。然而，随着AI/LLM代理驱动代码生成、调试和重构，"AI优先"的思维方式越来越相关。这涉及为LLM的限制（例如GPT-4o的128k令牌上下文窗口）进行设计，通过模块化代码以确保关键细节能够在该边界内。

- **优势**：清晰的分离关注点（例如，不同的数据流、提示或API边界）使AI能够更有效地进行推理。例如，AI工具如LangChain或自定义代理在逻辑不混杂的情况下表现更佳。
- **细节**：人类优先的设计仍然至关重要——AI仍然需要人类监督复杂领域，如金融，其中合规性和安全性至关重要。混合模型可能是最佳选择：AI优化用于重复性任务，人类优化用于关键逻辑。
- **总体**：大体同意；这一趋势在AI驱动的微服务和无服务器架构中已有体现。

#### 2. Spring提供了强大的抽象，但对AI/LLM理解构成挑战
您在这一点上是正确的。Spring（以及类似的Java框架如Micronaut）在企业环境中表现出色，具有依赖注入、AOP和分层抽象（例如控制器->服务->存储库）等功能。虽然在人类管理的大型团队中表现出色，但这些抽象可能会让LLM感到不堪重负，因为它们具有间接性和样板代码。

- **优势**：LLM通常难以处理深层调用栈或隐式行为（例如@Autowired注解），导致幻觉或不完整分析。关于AI代码生成的研究表明，在过度抽象的代码库中错误率更高。
- **细节**：并非所有抽象都是有害的——例如，接口增强了可测试性，间接帮助AI执行模拟生成等任务。然而，过度分层会增加上下文，使LLM难以追踪逻辑。
- **总体**：强烈同意；正在转向更轻量级的框架（例如Quarkus）或最小框架方法以提高AI兼容性。

#### 3. 偏好更扁平的结构，类似于扁平组织：限制在2级，第一级调用第二级，避免50级的深层堆栈
这是一个简化的有吸引力的想法，尽管并非普遍理想。更扁平的结构（例如，顶层编排器调用多个小函数）减少了嵌套，帮助LLM避免在复杂调用栈上进行推理错误。这类似于Python脚本中常见的简单函数链。

- **优势**：更扁平的代码降低了AI的认知负荷——LLM在线性或并行推理方面表现更好，而不是深层递归。"扁平组织"的类比成立：像初创公司一样，更扁平的代码更适应AI的修改。
- **细节**：从单一点调用大量函数可能会导致"意大利面条"代码，除非进行严格的组织（例如，清晰的命名或模块化）。在较大的系统中，最小的层次结构（3-4级）可以防止混乱。虽然AI代理如Devin可以很好地处理扁平结构，但如果没有适当的编排，可能会出现性能问题。
- **总体**：部分同意；在可行的地方扁平化是有益的，但需要测试可扩展性。这与AI驱动开发中的函数式编程趋势相符。

#### 4. AI/LLM难以处理复杂嵌套结构，但在小函数（100-200行）中表现出色；Python的调用和导入系统支持这一点
您在这一点上是准确的。当前的模型（例如Claude 3.5、GPT-4）擅长专注、封闭的任务，但在复杂性方面表现不佳——在超过约500行的上下文中，错误率会因令牌限制和注意力分散而增加。

- **优势**：小函数（100-200行）是AI的最佳选择：易于提示、生成或重构。Python的导入系统（例如`from module import func`）促进了模块化，使其比Java的类中心结构更适合AI。
- **细节**：虽然LLM正在进步（例如，通过思维链提示），但嵌套逻辑仍然是一个挑战。Python的灵活性有所帮助，但静态类型（例如TypeScript）也可以通过提供明确的提示来帮助AI。
- **总体**：强烈同意；这解释了为什么ML/AI生态系统（例如Hugging Face库）通常采用Python的模块化风格。

#### 5. 将Java大文件拆分为更多函数以便于测试/验证；Java项目应效仿Python的结构
这是一个实际的方向。大型、单体的Java类（例如1000+行）对人类和AI都是具有挑战性的，而将其拆分为更小的文件/函数可以提高粒度。

- **优势**：更小的单元简化了单元测试（例如使用JUnit）和验证（AI可以专注于一个函数），这与Python的每个功能一个模块的方法相似。构建工具如Maven/Gradle可以无缝适应这一点。
- **细节**：Java的包系统已经支持这一点，但从面向对象的单体到模块化的文化转变是必要的。并非所有Java项目都应该模仿Python——性能关键型应用可能从某些合并中受益。
- **总体**：同意；现代Java（例如Java 21中的记录和密封类）正朝着这个方向发展。

#### 6. 在AI/LLM时代，过程化编程可能比面向对象编程更出色
这是一个大胆但具有上下文相关性的观点。过程化或函数式方法，其强调直接流程和纯函数，与LLM的优势相符——生成线性代码比处理面向对象的状态、继承和多态更简单。

- **优势**：面向对象的抽象如深层继承通常会让LLM感到困惑，导致生成代码中的错误。过程化代码更可预测，适合AI的模式匹配性质。如Rust（具有过程化特征）和Go（强调简单性）等语言反映了这一趋势。
- **细节**：面向对象并未过时——它在建模复杂领域（例如金融实体）中仍然有效。混合方法（过程化核心与面向对象包装器）可能是理想的。通过定制提示，LLM可以管理面向对象，尽管过程化减少了摩擦。
- **总体**：部分同意；过程化/函数式风格在AI工作流程中正在兴起，但面向对象在大型系统的长期可维护性方面仍然有价值。

#### 7. 如VSCode或IntelliJ IDEA等IDE应提供AI辅助函数/方法编辑的快捷方式
您说得对，这将简化工作流程。虽然当前的IDE提供了部分支持（例如VSCode的"转到定义"或Copilot的内联建议），但专门的AI编辑快捷方式正在变得更加普遍。

- **优势**：选择一个函数/方法并提示AI（例如"重构此函数"）将提高效率。如Cursor（基于VSCode构建）等工具已经启用了区域特定的AI编辑。
- **细节**：IntelliJ IDEA提供了"提取方法"和AI助手（通过JetBrains AI），而VSCode扩展如GitHub Copilot Chat支持基于选择的查询。然而，原生的"AI编辑此函数"快捷方式将是一个有价值的补充——可能在未来更新中实现。
- **总体**：同意；这与AI优先工具的兴起趋势相符。

### 总结
您的见解大多是准确的：采用AI优化的架构（扁平、模块化、小单元）解决了实际的LLM限制，并可以加速开发。然而，极端的设计，如完全过程化或过度扁平化，可能会损害长期可维护性——平衡至关重要。在金融等领域，安全性和合规性可能需要某些抽象。您的观点与AI辅助编码的新兴最佳实践相符，其中简单性对人类和机器都有益。

如果您有具体的代码示例或想探索AI优先的重新设计，请随时提问！